return {
	id = "Card001",
	events = {
		{
			alpha = 0.6,
			style = {
				text = "電子対抗戦では、「画面任意の場所をタップ」すると、前衛艦船を移動させることが可能です。移動させると、「移動回数」を消費します。「移動回数」は時間経過で回復します",
				mode = 1,
				posY = -240,
				dir = -1,
				posX = 120,
				ui = {
					path = "UICamera/Canvas/UIMain/CombatUI(Clone)/CardPuzzleConsole/movedeck"
				}
			}
		},
		{
			alpha = 0.6,
			style = {
				text = "「コスト」が必要な分を満たしている場合、「コマンドカード」をドラッグ＆ドロップで使用可能。「コスト」は時間経過で回復します（最大10まで）",
				mode = 1,
				posY = -160,
				dir = 1,
				posX = 100,
				ui = {
					path = "UICamera/Canvas/UIMain/CombatUI(Clone)/CardPuzzleConsole/cardboard/CardTowerCardCombat(Clone)(Clone)"
				}
			}
		},
		{
			alpha = 0.6,
			style = {
				text = "「コマンドカード」を使用すると、デッキから自動で6まで1枚ずつ補充されます",
				mode = 1,
				posY = -160,
				dir = 1,
				posX = 100,
				ui = {
					path = "UICamera/Canvas/UIMain/CombatUI(Clone)/CardPuzzleConsole/cardboard/CardTowerCardCombat(Clone)(Clone)"
				}
			}
		},
		{
			alpha = 0.6,
			style = {
				text = "「コマンドカード」には「攻撃」「回復」「強化」など様々な効果がありますまた！一部のコマンドは使用するとデッキに戻されます！",
				mode = 1,
				posY = -160,
				dir = 1,
				posX = 100,
				ui = {
					path = "UICamera/Canvas/UIMain/CombatUI(Clone)/CardPuzzleConsole/cardboard/CardTowerCardCombat(Clone)(Clone)"
				}
			}
		}
	}
}
